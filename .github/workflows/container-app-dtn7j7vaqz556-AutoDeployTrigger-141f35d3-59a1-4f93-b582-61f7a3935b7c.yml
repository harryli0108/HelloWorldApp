name: Trigger auto deployment for container-app-dtn7j7vaqz556

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ test-branch ]
    paths:
    - 'src/**'
    - '.github/workflows/container-app-dtn7j7vaqz556-AutoDeployTrigger-141f35d3-59a1-4f93-b582-61f7a3935b7c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPDTN7J7VAQZ556_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPDTN7J7VAQZ556_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPDTN7J7VAQZ556_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/src 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPDTN7J7VAQZ556_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPDTN7J7VAQZ556_REGISTRY_PASSWORD }}
          containerAppName: container-app-dtn7j7vaqz556
          resourceGroup: helloworldappbbaa
          imageToBuild: default/container-app-dtn7j7vaqz556:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


